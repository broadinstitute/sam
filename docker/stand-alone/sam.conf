directory {
  url = "ldap://opendj:390"
  user = "cn=Directory Manager"
  password = "testtesttest"
  baseDn = "dc=example,dc=com"
  enabledUsersGroupDn = "cn=enabled-users,ou=groups,dc=example,dc=com"
}

swagger {
  googleClientId = clientid
  realm = google-project
}

schemaLock {
  lockSchemaOnBoot = true
  recheckTimeInterval = 5
  maxTimeToWait = 60
  instanceId = instance1
  schemaVersion = 1
}

emailDomain = "dev.test.firecloud.org"

petServiceAccount {
  # TODO: eventually pet service accounts should live in their own separate project (managed by us)
  googleProject = "broad-dsde-dev"
  serviceAccountUsers = ["leonardo-dev@broad-dsde-dev.iam.gserviceaccount.com"]
}